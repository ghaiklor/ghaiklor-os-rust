name: Rust

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: cargo xbuild --verbose
    - name: Run tests
      run: cargo xtest --verbose
