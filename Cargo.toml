[package]
name = "ghaiklor-os-rust"
version = "0.1.0"
authors = ["Eugene Obrezkov <ghaiklor@gmail.com>"]

[lib]
crate-type = ["staticlib"]

[dependencies]
rlibc = "0.1.4"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
